// Get the client and invoice models
const clientModel = require('./model.client')
const invoiceModel = require('./model.invoice')

// Export the modesl
module.exports = {
  clientModel,
  invoiceModel
}
